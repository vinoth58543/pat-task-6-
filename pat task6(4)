# Input an integer
num = int(input("Enter an integer: "))

# Convert the integer to a string to work with its digits
num_str = str(num)

# Get the first digit (character) and convert it back to an integer
first_digit = int(num_str[0])

# Get the last digit (character) and convert it back to an integer
last_digit = int(num_str[-1])

# Calculate the sum of the first and last digits
sum_of_digits = first_digit + last_digit

# Print the result
print("Sum of the first and last digits:", sum_of_digits)
